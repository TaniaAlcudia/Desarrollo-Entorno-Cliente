//Escribe un programa que contenga un formulario con cinco botones. Cada uno de 
//los botones debe tener como etiqueta el nombre de un color y al pulsarlo pondrá 
//el color del fondo del documento del mismo color que indica.

function cambiarColor(color)
{
    document.body.style.backgroundColor = color;
}